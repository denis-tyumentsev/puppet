class zabbix_up::params {
#  $f_zbx_key_name  = 'ntp.offset'
#  $zbx_filename = 'ntp.offset'
#  $f_zbx_key_name  = 'puppet.version'
#  $zbx_filename = 'puppet.version'
   $f_zbx_key_name  = 'nginx[*]'
   $zbx_filename = 'nginx_stats'
}
